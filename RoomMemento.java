/*public class RoomMemento implements Memento{
    private Building build;
    private int length,width,id;
    
    public RoomMemento(Building build,int len, int wid){
        this.build = build;
        this.length = len;
        this.width = wid;
        this.id = build.getId();
    }
    public void restore(){
        build.addRoom(length, width);
    }
    
    public int getBuildingID(){
        return id;
    }
}
*/